# support-firecloud-ubuntu-xenial-minimal:
sfuxm:
  build:
    dockerfile: .codeship/ubuntu-xenial/Dockerfile
    args:
  environment:
    SF_CI_BREW_INSTALL: minimal
    SF_LOG_BOOTSTRAP: true

# support-firecloud-ubuntu-xenial-dev:
sfuxd:
  build:
    dockerfile: .codeship/ubuntu-xenial/Dockerfile
    args:
  environment:
    SF_CI_BREW_INSTALL: dev
    SF_LOG_BOOTSTRAP: true

# support-firecloud-ubuntu-bionic-minimal:
sfubm:
  build:
    dockerfile: .codeship/ubuntu-bionic/Dockerfile
    args:
  environment:
    SF_CI_BREW_INSTALL: minimal
    SF_LOG_BOOTSTRAP: true

# support-firecloud-ubuntu-bionic-dev:
sfubd:
  build:
    dockerfile: .codeship/ubuntu-bionic/Dockerfile
    args:
  environment:
    SF_CI_BREW_INSTALL: dev
    SF_LOG_BOOTSTRAP: true
