{
  "private": true,
  "name": "support-firecloud",
  "description": "Software and configuration that support TobiiPro's Cloud Services development.",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "devDependencies": {
    "eclint": "2.3.0"
  },
  "scripts": {
    "is-decrypted": "cat docs/manage-secrets.md.test.secret | grep -q 'This is a test of transcrypt.'",
    "test": "eclint check $(git ls-files | grep -v -e \"^package-lock.json$\" -e \"^LICENSE$\" -e \"^UNLICENSE$\" -e \"^bin/\" -e \"^transcrypt$\")"
  }
}
