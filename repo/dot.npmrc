# use `npm install --ignore-lock` when/if needed,
# otherwise generating this (commiting it actually) is a headache
package-lock=false

# package-lock or not, take control over upgrades
save-exact=true

# common-sense; already by default in npm v5
save=true
