# use `npm install --ignore-lock` when/if needed,
# otherwise generating this (commiting it actually) is a headache
package-lock=false

# https://blog.uidrafter.com/engineering/getting-rid-of-npm-scripts
ignore-scripts=true

# hide update notifier
update-notifier=false
